import React from 'react';
import CREATE_USERS from "../helpers/Mutations"
import {
  useMutation,
} from "@apollo/client";

export default function Users() {
  const { } = useMutation(CREATE_USERS);

    return
    <>
    </>
    
}